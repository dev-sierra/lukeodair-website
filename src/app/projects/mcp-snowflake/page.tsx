export default function McpSnowflake() {
  return (
    <main className="mx-auto flex w-full max-w-3xl flex-1 flex-col gap-6 px-6 py-16">
      <h1 className="text-2xl font-semibold tracking-tight">
        MCP Server for Snowflake
      </h1>
      <p className="text-sm uppercase tracking-wide text-zinc-500">
        In progress — Aug 2026
      </p>
      <p className="text-lg leading-relaxed text-zinc-700 dark:text-zinc-300">
        An open-source MCP server for Snowflake with PII-safe dynamic
        masking: read-only queries, masking enforcement, and role-based
        access. Design writeup and repo link landing here once the project
        starts.
      </p>
    </main>
  );
}
